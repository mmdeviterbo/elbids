import { ReactElement, useEffect} from 'react';
import { NextPage } from 'next';
import { useRouter } from 'next/router';

const NotificationBell: NextPage = (): ReactElement=> {
  return(
    <>
      <h1>Notification here</h1>
    </>
  )
}
export default NotificationBell
